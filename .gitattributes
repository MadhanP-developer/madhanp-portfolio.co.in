# Normalize line endings to LF on commit (prevents CRLF/LF churn across OSes)
* text=auto eol=lf

# Binary assets — never normalize
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
